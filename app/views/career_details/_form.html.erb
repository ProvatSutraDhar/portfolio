
<div class="col-md-12">
<%= form_with(model: career_detail) do |form| %>
  <% if career_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(career_detail.errors.count, "error") %> prohibited this career_detail from being saved:</h2>

      <ul>
        <% career_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-lg-12">
    <%= form.text_area :heading, class:"form-control", placeholder:"Carrer Objectives", required:true %>
  </div>

  <div class="col-lg-12">
    <%= form.text_area :details, class:"form-control", placeholder:"Carrer Objectives in Details", required:true %>
  </div>
    <div class="col-lg-12">
      <%=form.label :photo%>
      <%= form.file_field :photo, class:"form-control" %>
      <%=form.hidden_field :photo_cache%>

      <%if @career_detail.photo.present?%>
      <%=image_tag @career_detail.photo.thumb.url%>
      <%end%>
    </div>

  <div>
    <%= form.submit class:"btn btn-defeault" %>
  </div>
<% end %>
</div>
