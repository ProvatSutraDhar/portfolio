<%= form_with(model: career_detail) do |form| %>
  <% if career_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(career_detail.errors.count, "error") %> prohibited this career_detail from being saved:</h2>

      <ul>
        <% career_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :heading, style: "display: block" %>
    <%= form.text_area :heading %>
  </div>

  <div>
    <%= form.label :details, style: "display: block" %>
    <%= form.text_area :details %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
