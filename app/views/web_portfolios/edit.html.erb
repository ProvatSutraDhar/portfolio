<%if current_user%>
    <%if current_user.admin?%>
<center class="mt-20">
  <div class="card col-md-8">
    <div class="card-header">
      <h1>Editing web portfolio</h1>
    </div>
    <div class="card-body">
      <%= render "form", web_portfolio: @web_portfolio %>
      <br>
<div>
  <%= link_to "Show this web portfolio", @web_portfolio %> |
  <%= link_to "Back to web portfolios", web_portfolios_path %>
</div>
  </div>
</center>
<%end%>
<%end%>
